<?php

/**
 * Base class that represents a row from the 'tk_jurusan' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Thu Oct 17 01:36:52 2013
 *
 * @package    lib.model.perwalian_ft.om
 */
abstract class BaseJurusan extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        JurusanPeer
	 */
	protected static $peer;

	/**
	 * The value for the kode_jur field.
	 * @var        string
	 */
	protected $kode_jur;

	/**
	 * The value for the nama field.
	 * @var        string
	 */
	protected $nama;

	/**
	 * @var        array Admin[] Collection to store aggregation of Admin objects.
	 */
	protected $collAdmins;

	/**
	 * @var        Criteria The criteria used to select the current contents of collAdmins.
	 */
	private $lastAdminCriteria = null;

	/**
	 * @var        array Dosen[] Collection to store aggregation of Dosen objects.
	 */
	protected $collDosens;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDosens.
	 */
	private $lastDosenCriteria = null;

	/**
	 * @var        array KelasJurusan[] Collection to store aggregation of KelasJurusan objects.
	 */
	protected $collKelasJurusans;

	/**
	 * @var        Criteria The criteria used to select the current contents of collKelasJurusans.
	 */
	private $lastKelasJurusanCriteria = null;

	/**
	 * @var        array MataKuliahJurusan[] Collection to store aggregation of MataKuliahJurusan objects.
	 */
	protected $collMataKuliahJurusans;

	/**
	 * @var        Criteria The criteria used to select the current contents of collMataKuliahJurusans.
	 */
	private $lastMataKuliahJurusanCriteria = null;

	/**
	 * @var        array Karyawan[] Collection to store aggregation of Karyawan objects.
	 */
	protected $collKaryawans;

	/**
	 * @var        Criteria The criteria used to select the current contents of collKaryawans.
	 */
	private $lastKaryawanCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'JurusanPeer';

	/**
	 * Get the [kode_jur] column value.
	 * 
	 * @return     string
	 */
	public function getKodeJur()
	{
		return $this->kode_jur;
	}

	/**
	 * Get the [nama] column value.
	 * 
	 * @return     string
	 */
	public function getNama()
	{
		return $this->nama;
	}

	/**
	 * Set the value of [kode_jur] column.
	 * 
	 * @param      string $v new value
	 * @return     Jurusan The current object (for fluent API support)
	 */
	public function setKodeJur($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->kode_jur !== $v) {
			$this->kode_jur = $v;
			$this->modifiedColumns[] = JurusanPeer::KODE_JUR;
		}

		return $this;
	} // setKodeJur()

	/**
	 * Set the value of [nama] column.
	 * 
	 * @param      string $v new value
	 * @return     Jurusan The current object (for fluent API support)
	 */
	public function setNama($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nama !== $v) {
			$this->nama = $v;
			$this->modifiedColumns[] = JurusanPeer::NAMA;
		}

		return $this;
	} // setNama()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->kode_jur = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->nama = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 2; // 2 = JurusanPeer::NUM_COLUMNS - JurusanPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Jurusan object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(JurusanPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = JurusanPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->collAdmins = null;
			$this->lastAdminCriteria = null;

			$this->collDosens = null;
			$this->lastDosenCriteria = null;

			$this->collKelasJurusans = null;
			$this->lastKelasJurusanCriteria = null;

			$this->collMataKuliahJurusans = null;
			$this->lastMataKuliahJurusanCriteria = null;

			$this->collKaryawans = null;
			$this->lastKaryawanCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(JurusanPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseJurusan:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				JurusanPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseJurusan:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(JurusanPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseJurusan:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseJurusan:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				JurusanPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = JurusanPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += JurusanPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collAdmins !== null) {
				foreach ($this->collAdmins as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collDosens !== null) {
				foreach ($this->collDosens as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collKelasJurusans !== null) {
				foreach ($this->collKelasJurusans as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collMataKuliahJurusans !== null) {
				foreach ($this->collMataKuliahJurusans as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collKaryawans !== null) {
				foreach ($this->collKaryawans as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			if (($retval = JurusanPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collAdmins !== null) {
					foreach ($this->collAdmins as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collDosens !== null) {
					foreach ($this->collDosens as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collKelasJurusans !== null) {
					foreach ($this->collKelasJurusans as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collMataKuliahJurusans !== null) {
					foreach ($this->collMataKuliahJurusans as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collKaryawans !== null) {
					foreach ($this->collKaryawans as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = JurusanPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getKodeJur();
				break;
			case 1:
				return $this->getNama();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = JurusanPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getKodeJur(),
			$keys[1] => $this->getNama(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = JurusanPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setKodeJur($value);
				break;
			case 1:
				$this->setNama($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = JurusanPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setKodeJur($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setNama($arr[$keys[1]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(JurusanPeer::DATABASE_NAME);

		if ($this->isColumnModified(JurusanPeer::KODE_JUR)) $criteria->add(JurusanPeer::KODE_JUR, $this->kode_jur);
		if ($this->isColumnModified(JurusanPeer::NAMA)) $criteria->add(JurusanPeer::NAMA, $this->nama);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(JurusanPeer::DATABASE_NAME);

		$criteria->add(JurusanPeer::KODE_JUR, $this->kode_jur);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getKodeJur();
	}

	/**
	 * Generic method to set the primary key (kode_jur column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setKodeJur($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Jurusan (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setKodeJur($this->kode_jur);

		$copyObj->setNama($this->nama);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getAdmins() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addAdmin($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getDosens() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDosen($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getKelasJurusans() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addKelasJurusan($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getMataKuliahJurusans() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addMataKuliahJurusan($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getKaryawans() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addKaryawan($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Jurusan Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     JurusanPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new JurusanPeer();
		}
		return self::$peer;
	}

	/**
	 * Clears out the collAdmins collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addAdmins()
	 */
	public function clearAdmins()
	{
		$this->collAdmins = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collAdmins collection (array).
	 *
	 * By default this just sets the collAdmins collection to an empty array (like clearcollAdmins());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initAdmins()
	{
		$this->collAdmins = array();
	}

	/**
	 * Gets an array of Admin objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Jurusan has previously been saved, it will retrieve
	 * related Admins from storage. If this Jurusan is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Admin[]
	 * @throws     PropelException
	 */
	public function getAdmins($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collAdmins === null) {
			if ($this->isNew()) {
			   $this->collAdmins = array();
			} else {

				$criteria->add(AdminPeer::KODE_JUR, $this->kode_jur);

				AdminPeer::addSelectColumns($criteria);
				$this->collAdmins = AdminPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(AdminPeer::KODE_JUR, $this->kode_jur);

				AdminPeer::addSelectColumns($criteria);
				if (!isset($this->lastAdminCriteria) || !$this->lastAdminCriteria->equals($criteria)) {
					$this->collAdmins = AdminPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastAdminCriteria = $criteria;
		return $this->collAdmins;
	}

	/**
	 * Returns the number of related Admin objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Admin objects.
	 * @throws     PropelException
	 */
	public function countAdmins(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collAdmins === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(AdminPeer::KODE_JUR, $this->kode_jur);

				$count = AdminPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(AdminPeer::KODE_JUR, $this->kode_jur);

				if (!isset($this->lastAdminCriteria) || !$this->lastAdminCriteria->equals($criteria)) {
					$count = AdminPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collAdmins);
				}
			} else {
				$count = count($this->collAdmins);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Admin object to this object
	 * through the Admin foreign key attribute.
	 *
	 * @param      Admin $l Admin
	 * @return     void
	 * @throws     PropelException
	 */
	public function addAdmin(Admin $l)
	{
		if ($this->collAdmins === null) {
			$this->initAdmins();
		}
		if (!in_array($l, $this->collAdmins, true)) { // only add it if the **same** object is not already associated
			array_push($this->collAdmins, $l);
			$l->setJurusan($this);
		}
	}

	/**
	 * Clears out the collDosens collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDosens()
	 */
	public function clearDosens()
	{
		$this->collDosens = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDosens collection (array).
	 *
	 * By default this just sets the collDosens collection to an empty array (like clearcollDosens());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDosens()
	{
		$this->collDosens = array();
	}

	/**
	 * Gets an array of Dosen objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Jurusan has previously been saved, it will retrieve
	 * related Dosens from storage. If this Jurusan is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Dosen[]
	 * @throws     PropelException
	 */
	public function getDosens($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDosens === null) {
			if ($this->isNew()) {
			   $this->collDosens = array();
			} else {

				$criteria->add(DosenPeer::KODE_JUR, $this->kode_jur);

				DosenPeer::addSelectColumns($criteria);
				$this->collDosens = DosenPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DosenPeer::KODE_JUR, $this->kode_jur);

				DosenPeer::addSelectColumns($criteria);
				if (!isset($this->lastDosenCriteria) || !$this->lastDosenCriteria->equals($criteria)) {
					$this->collDosens = DosenPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDosenCriteria = $criteria;
		return $this->collDosens;
	}

	/**
	 * Returns the number of related Dosen objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Dosen objects.
	 * @throws     PropelException
	 */
	public function countDosens(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDosens === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DosenPeer::KODE_JUR, $this->kode_jur);

				$count = DosenPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DosenPeer::KODE_JUR, $this->kode_jur);

				if (!isset($this->lastDosenCriteria) || !$this->lastDosenCriteria->equals($criteria)) {
					$count = DosenPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDosens);
				}
			} else {
				$count = count($this->collDosens);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Dosen object to this object
	 * through the Dosen foreign key attribute.
	 *
	 * @param      Dosen $l Dosen
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDosen(Dosen $l)
	{
		if ($this->collDosens === null) {
			$this->initDosens();
		}
		if (!in_array($l, $this->collDosens, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDosens, $l);
			$l->setJurusan($this);
		}
	}

	/**
	 * Clears out the collKelasJurusans collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addKelasJurusans()
	 */
	public function clearKelasJurusans()
	{
		$this->collKelasJurusans = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collKelasJurusans collection (array).
	 *
	 * By default this just sets the collKelasJurusans collection to an empty array (like clearcollKelasJurusans());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initKelasJurusans()
	{
		$this->collKelasJurusans = array();
	}

	/**
	 * Gets an array of KelasJurusan objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Jurusan has previously been saved, it will retrieve
	 * related KelasJurusans from storage. If this Jurusan is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array KelasJurusan[]
	 * @throws     PropelException
	 */
	public function getKelasJurusans($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collKelasJurusans === null) {
			if ($this->isNew()) {
			   $this->collKelasJurusans = array();
			} else {

				$criteria->add(KelasJurusanPeer::KODE_JUR, $this->kode_jur);

				KelasJurusanPeer::addSelectColumns($criteria);
				$this->collKelasJurusans = KelasJurusanPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(KelasJurusanPeer::KODE_JUR, $this->kode_jur);

				KelasJurusanPeer::addSelectColumns($criteria);
				if (!isset($this->lastKelasJurusanCriteria) || !$this->lastKelasJurusanCriteria->equals($criteria)) {
					$this->collKelasJurusans = KelasJurusanPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastKelasJurusanCriteria = $criteria;
		return $this->collKelasJurusans;
	}

	/**
	 * Returns the number of related KelasJurusan objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related KelasJurusan objects.
	 * @throws     PropelException
	 */
	public function countKelasJurusans(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collKelasJurusans === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(KelasJurusanPeer::KODE_JUR, $this->kode_jur);

				$count = KelasJurusanPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(KelasJurusanPeer::KODE_JUR, $this->kode_jur);

				if (!isset($this->lastKelasJurusanCriteria) || !$this->lastKelasJurusanCriteria->equals($criteria)) {
					$count = KelasJurusanPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collKelasJurusans);
				}
			} else {
				$count = count($this->collKelasJurusans);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a KelasJurusan object to this object
	 * through the KelasJurusan foreign key attribute.
	 *
	 * @param      KelasJurusan $l KelasJurusan
	 * @return     void
	 * @throws     PropelException
	 */
	public function addKelasJurusan(KelasJurusan $l)
	{
		if ($this->collKelasJurusans === null) {
			$this->initKelasJurusans();
		}
		if (!in_array($l, $this->collKelasJurusans, true)) { // only add it if the **same** object is not already associated
			array_push($this->collKelasJurusans, $l);
			$l->setJurusan($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Jurusan is new, it will return
	 * an empty collection; or if this Jurusan has previously
	 * been saved, it will retrieve related KelasJurusans from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Jurusan.
	 */
	public function getKelasJurusansJoinKelasMK($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collKelasJurusans === null) {
			if ($this->isNew()) {
				$this->collKelasJurusans = array();
			} else {

				$criteria->add(KelasJurusanPeer::KODE_JUR, $this->kode_jur);

				$this->collKelasJurusans = KelasJurusanPeer::doSelectJoinKelasMK($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(KelasJurusanPeer::KODE_JUR, $this->kode_jur);

			if (!isset($this->lastKelasJurusanCriteria) || !$this->lastKelasJurusanCriteria->equals($criteria)) {
				$this->collKelasJurusans = KelasJurusanPeer::doSelectJoinKelasMK($criteria, $con, $join_behavior);
			}
		}
		$this->lastKelasJurusanCriteria = $criteria;

		return $this->collKelasJurusans;
	}

	/**
	 * Clears out the collMataKuliahJurusans collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addMataKuliahJurusans()
	 */
	public function clearMataKuliahJurusans()
	{
		$this->collMataKuliahJurusans = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collMataKuliahJurusans collection (array).
	 *
	 * By default this just sets the collMataKuliahJurusans collection to an empty array (like clearcollMataKuliahJurusans());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initMataKuliahJurusans()
	{
		$this->collMataKuliahJurusans = array();
	}

	/**
	 * Gets an array of MataKuliahJurusan objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Jurusan has previously been saved, it will retrieve
	 * related MataKuliahJurusans from storage. If this Jurusan is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array MataKuliahJurusan[]
	 * @throws     PropelException
	 */
	public function getMataKuliahJurusans($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMataKuliahJurusans === null) {
			if ($this->isNew()) {
			   $this->collMataKuliahJurusans = array();
			} else {

				$criteria->add(MataKuliahJurusanPeer::KODE_JUR, $this->kode_jur);

				MataKuliahJurusanPeer::addSelectColumns($criteria);
				$this->collMataKuliahJurusans = MataKuliahJurusanPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(MataKuliahJurusanPeer::KODE_JUR, $this->kode_jur);

				MataKuliahJurusanPeer::addSelectColumns($criteria);
				if (!isset($this->lastMataKuliahJurusanCriteria) || !$this->lastMataKuliahJurusanCriteria->equals($criteria)) {
					$this->collMataKuliahJurusans = MataKuliahJurusanPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastMataKuliahJurusanCriteria = $criteria;
		return $this->collMataKuliahJurusans;
	}

	/**
	 * Returns the number of related MataKuliahJurusan objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related MataKuliahJurusan objects.
	 * @throws     PropelException
	 */
	public function countMataKuliahJurusans(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collMataKuliahJurusans === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(MataKuliahJurusanPeer::KODE_JUR, $this->kode_jur);

				$count = MataKuliahJurusanPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(MataKuliahJurusanPeer::KODE_JUR, $this->kode_jur);

				if (!isset($this->lastMataKuliahJurusanCriteria) || !$this->lastMataKuliahJurusanCriteria->equals($criteria)) {
					$count = MataKuliahJurusanPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collMataKuliahJurusans);
				}
			} else {
				$count = count($this->collMataKuliahJurusans);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a MataKuliahJurusan object to this object
	 * through the MataKuliahJurusan foreign key attribute.
	 *
	 * @param      MataKuliahJurusan $l MataKuliahJurusan
	 * @return     void
	 * @throws     PropelException
	 */
	public function addMataKuliahJurusan(MataKuliahJurusan $l)
	{
		if ($this->collMataKuliahJurusans === null) {
			$this->initMataKuliahJurusans();
		}
		if (!in_array($l, $this->collMataKuliahJurusans, true)) { // only add it if the **same** object is not already associated
			array_push($this->collMataKuliahJurusans, $l);
			$l->setJurusan($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Jurusan is new, it will return
	 * an empty collection; or if this Jurusan has previously
	 * been saved, it will retrieve related MataKuliahJurusans from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Jurusan.
	 */
	public function getMataKuliahJurusansJoinMataKuliah($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collMataKuliahJurusans === null) {
			if ($this->isNew()) {
				$this->collMataKuliahJurusans = array();
			} else {

				$criteria->add(MataKuliahJurusanPeer::KODE_JUR, $this->kode_jur);

				$this->collMataKuliahJurusans = MataKuliahJurusanPeer::doSelectJoinMataKuliah($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(MataKuliahJurusanPeer::KODE_JUR, $this->kode_jur);

			if (!isset($this->lastMataKuliahJurusanCriteria) || !$this->lastMataKuliahJurusanCriteria->equals($criteria)) {
				$this->collMataKuliahJurusans = MataKuliahJurusanPeer::doSelectJoinMataKuliah($criteria, $con, $join_behavior);
			}
		}
		$this->lastMataKuliahJurusanCriteria = $criteria;

		return $this->collMataKuliahJurusans;
	}

	/**
	 * Clears out the collKaryawans collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addKaryawans()
	 */
	public function clearKaryawans()
	{
		$this->collKaryawans = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collKaryawans collection (array).
	 *
	 * By default this just sets the collKaryawans collection to an empty array (like clearcollKaryawans());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initKaryawans()
	{
		$this->collKaryawans = array();
	}

	/**
	 * Gets an array of Karyawan objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Jurusan has previously been saved, it will retrieve
	 * related Karyawans from storage. If this Jurusan is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Karyawan[]
	 * @throws     PropelException
	 */
	public function getKaryawans($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collKaryawans === null) {
			if ($this->isNew()) {
			   $this->collKaryawans = array();
			} else {

				$criteria->add(KaryawanPeer::KODE_JUR, $this->kode_jur);

				KaryawanPeer::addSelectColumns($criteria);
				$this->collKaryawans = KaryawanPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(KaryawanPeer::KODE_JUR, $this->kode_jur);

				KaryawanPeer::addSelectColumns($criteria);
				if (!isset($this->lastKaryawanCriteria) || !$this->lastKaryawanCriteria->equals($criteria)) {
					$this->collKaryawans = KaryawanPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastKaryawanCriteria = $criteria;
		return $this->collKaryawans;
	}

	/**
	 * Returns the number of related Karyawan objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Karyawan objects.
	 * @throws     PropelException
	 */
	public function countKaryawans(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(JurusanPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collKaryawans === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(KaryawanPeer::KODE_JUR, $this->kode_jur);

				$count = KaryawanPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(KaryawanPeer::KODE_JUR, $this->kode_jur);

				if (!isset($this->lastKaryawanCriteria) || !$this->lastKaryawanCriteria->equals($criteria)) {
					$count = KaryawanPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collKaryawans);
				}
			} else {
				$count = count($this->collKaryawans);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Karyawan object to this object
	 * through the Karyawan foreign key attribute.
	 *
	 * @param      Karyawan $l Karyawan
	 * @return     void
	 * @throws     PropelException
	 */
	public function addKaryawan(Karyawan $l)
	{
		if ($this->collKaryawans === null) {
			$this->initKaryawans();
		}
		if (!in_array($l, $this->collKaryawans, true)) { // only add it if the **same** object is not already associated
			array_push($this->collKaryawans, $l);
			$l->setJurusan($this);
		}
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collAdmins) {
				foreach ((array) $this->collAdmins as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collDosens) {
				foreach ((array) $this->collDosens as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collKelasJurusans) {
				foreach ((array) $this->collKelasJurusans as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collMataKuliahJurusans) {
				foreach ((array) $this->collMataKuliahJurusans as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collKaryawans) {
				foreach ((array) $this->collKaryawans as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collAdmins = null;
		$this->collDosens = null;
		$this->collKelasJurusans = null;
		$this->collMataKuliahJurusans = null;
		$this->collKaryawans = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseJurusan:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseJurusan::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseJurusan

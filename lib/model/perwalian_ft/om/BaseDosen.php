<?php

/**
 * Base class that represents a row from the 'tk_dosen' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.2 on:
 *
 * Thu Oct 17 01:36:51 2013
 *
 * @package    lib.model.perwalian_ft.om
 */
abstract class BaseDosen extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        DosenPeer
	 */
	protected static $peer;

	/**
	 * The value for the kode_dosen field.
	 * @var        string
	 */
	protected $kode_dosen;

	/**
	 * The value for the nama field.
	 * @var        string
	 */
	protected $nama;

	/**
	 * The value for the status field.
	 * @var        string
	 */
	protected $status;

	/**
	 * The value for the kode_jur field.
	 * @var        string
	 */
	protected $kode_jur;

	/**
	 * The value for the is_pengawas field.
	 * Note: this column has a database default value of: true
	 * @var        boolean
	 */
	protected $is_pengawas;

	/**
	 * The value for the npk field.
	 * @var        string
	 */
	protected $npk;

	/**
	 * @var        Jurusan
	 */
	protected $aJurusan;

	/**
	 * @var        array DosenKelas[] Collection to store aggregation of DosenKelas objects.
	 */
	protected $collDosenKelass;

	/**
	 * @var        Criteria The criteria used to select the current contents of collDosenKelass.
	 */
	private $lastDosenKelasCriteria = null;

	/**
	 * @var        DosenNonJaga one-to-one related DosenNonJaga object
	 */
	protected $singleDosenNonJaga;

	/**
	 * @var        array JadwalRuang[] Collection to store aggregation of JadwalRuang objects.
	 */
	protected $collJadwalRuangs;

	/**
	 * @var        Criteria The criteria used to select the current contents of collJadwalRuangs.
	 */
	private $lastJadwalRuangCriteria = null;

	/**
	 * @var        array PiketUjian[] Collection to store aggregation of PiketUjian objects.
	 */
	protected $collPiketUjians;

	/**
	 * @var        Criteria The criteria used to select the current contents of collPiketUjians.
	 */
	private $lastPiketUjianCriteria = null;

	/**
	 * @var        array SoalUjian[] Collection to store aggregation of SoalUjian objects.
	 */
	protected $collSoalUjians;

	/**
	 * @var        Criteria The criteria used to select the current contents of collSoalUjians.
	 */
	private $lastSoalUjianCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'DosenPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->is_pengawas = true;
	}

	/**
	 * Initializes internal state of BaseDosen object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [kode_dosen] column value.
	 * 
	 * @return     string
	 */
	public function getKodeDosen()
	{
		return $this->kode_dosen;
	}

	/**
	 * Get the [nama] column value.
	 * 
	 * @return     string
	 */
	public function getNama()
	{
		return $this->nama;
	}

	/**
	 * Get the [status] column value.
	 * 
	 * @return     string
	 */
	public function getStatus()
	{
		return $this->status;
	}

	/**
	 * Get the [kode_jur] column value.
	 * 
	 * @return     string
	 */
	public function getKodeJur()
	{
		return $this->kode_jur;
	}

	/**
	 * Get the [is_pengawas] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsPengawas()
	{
		return $this->is_pengawas;
	}

	/**
	 * Get the [npk] column value.
	 * 
	 * @return     string
	 */
	public function getNpk()
	{
		return $this->npk;
	}

	/**
	 * Set the value of [kode_dosen] column.
	 * 
	 * @param      string $v new value
	 * @return     Dosen The current object (for fluent API support)
	 */
	public function setKodeDosen($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->kode_dosen !== $v) {
			$this->kode_dosen = $v;
			$this->modifiedColumns[] = DosenPeer::KODE_DOSEN;
		}

		return $this;
	} // setKodeDosen()

	/**
	 * Set the value of [nama] column.
	 * 
	 * @param      string $v new value
	 * @return     Dosen The current object (for fluent API support)
	 */
	public function setNama($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->nama !== $v) {
			$this->nama = $v;
			$this->modifiedColumns[] = DosenPeer::NAMA;
		}

		return $this;
	} // setNama()

	/**
	 * Set the value of [status] column.
	 * 
	 * @param      string $v new value
	 * @return     Dosen The current object (for fluent API support)
	 */
	public function setStatus($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->status !== $v) {
			$this->status = $v;
			$this->modifiedColumns[] = DosenPeer::STATUS;
		}

		return $this;
	} // setStatus()

	/**
	 * Set the value of [kode_jur] column.
	 * 
	 * @param      string $v new value
	 * @return     Dosen The current object (for fluent API support)
	 */
	public function setKodeJur($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->kode_jur !== $v) {
			$this->kode_jur = $v;
			$this->modifiedColumns[] = DosenPeer::KODE_JUR;
		}

		if ($this->aJurusan !== null && $this->aJurusan->getKodeJur() !== $v) {
			$this->aJurusan = null;
		}

		return $this;
	} // setKodeJur()

	/**
	 * Set the value of [is_pengawas] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Dosen The current object (for fluent API support)
	 */
	public function setIsPengawas($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_pengawas !== $v || $this->isNew()) {
			$this->is_pengawas = $v;
			$this->modifiedColumns[] = DosenPeer::IS_PENGAWAS;
		}

		return $this;
	} // setIsPengawas()

	/**
	 * Set the value of [npk] column.
	 * 
	 * @param      string $v new value
	 * @return     Dosen The current object (for fluent API support)
	 */
	public function setNpk($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->npk !== $v) {
			$this->npk = $v;
			$this->modifiedColumns[] = DosenPeer::NPK;
		}

		return $this;
	} // setNpk()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->is_pengawas !== true) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->kode_dosen = ($row[$startcol + 0] !== null) ? (string) $row[$startcol + 0] : null;
			$this->nama = ($row[$startcol + 1] !== null) ? (string) $row[$startcol + 1] : null;
			$this->status = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->kode_jur = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->is_pengawas = ($row[$startcol + 4] !== null) ? (boolean) $row[$startcol + 4] : null;
			$this->npk = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 6; // 6 = DosenPeer::NUM_COLUMNS - DosenPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Dosen object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aJurusan !== null && $this->kode_jur !== $this->aJurusan->getKodeJur()) {
			$this->aJurusan = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(DosenPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = DosenPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aJurusan = null;
			$this->collDosenKelass = null;
			$this->lastDosenKelasCriteria = null;

			$this->singleDosenNonJaga = null;

			$this->collJadwalRuangs = null;
			$this->lastJadwalRuangCriteria = null;

			$this->collPiketUjians = null;
			$this->lastPiketUjianCriteria = null;

			$this->collSoalUjians = null;
			$this->lastSoalUjianCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(DosenPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseDosen:delete:pre') as $callable)
			{
			  if (call_user_func($callable, $this, $con))
			  {
			    $con->commit();
			
			    return;
			  }
			}

			if ($ret) {
				DosenPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseDosen:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			} else {
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(DosenPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseDosen:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    $con->commit();
			
			    return $affectedRows;
			  }
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseDosen:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				DosenPeer::addInstanceToPool($this);
			} else {
				$affectedRows = 0;
			}
			$con->commit();
			return $affectedRows;
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aJurusan !== null) {
				if ($this->aJurusan->isModified() || $this->aJurusan->isNew()) {
					$affectedRows += $this->aJurusan->save($con);
				}
				$this->setJurusan($this->aJurusan);
			}


			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = DosenPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setNew(false);
				} else {
					$affectedRows += DosenPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collDosenKelass !== null) {
				foreach ($this->collDosenKelass as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->singleDosenNonJaga !== null) {
				if (!$this->singleDosenNonJaga->isDeleted()) {
						$affectedRows += $this->singleDosenNonJaga->save($con);
				}
			}

			if ($this->collJadwalRuangs !== null) {
				foreach ($this->collJadwalRuangs as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collPiketUjians !== null) {
				foreach ($this->collPiketUjians as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collSoalUjians !== null) {
				foreach ($this->collSoalUjians as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aJurusan !== null) {
				if (!$this->aJurusan->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aJurusan->getValidationFailures());
				}
			}


			if (($retval = DosenPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collDosenKelass !== null) {
					foreach ($this->collDosenKelass as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->singleDosenNonJaga !== null) {
					if (!$this->singleDosenNonJaga->validate($columns)) {
						$failureMap = array_merge($failureMap, $this->singleDosenNonJaga->getValidationFailures());
					}
				}

				if ($this->collJadwalRuangs !== null) {
					foreach ($this->collJadwalRuangs as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collPiketUjians !== null) {
					foreach ($this->collPiketUjians as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collSoalUjians !== null) {
					foreach ($this->collSoalUjians as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = DosenPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getKodeDosen();
				break;
			case 1:
				return $this->getNama();
				break;
			case 2:
				return $this->getStatus();
				break;
			case 3:
				return $this->getKodeJur();
				break;
			case 4:
				return $this->getIsPengawas();
				break;
			case 5:
				return $this->getNpk();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = DosenPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getKodeDosen(),
			$keys[1] => $this->getNama(),
			$keys[2] => $this->getStatus(),
			$keys[3] => $this->getKodeJur(),
			$keys[4] => $this->getIsPengawas(),
			$keys[5] => $this->getNpk(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = DosenPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setKodeDosen($value);
				break;
			case 1:
				$this->setNama($value);
				break;
			case 2:
				$this->setStatus($value);
				break;
			case 3:
				$this->setKodeJur($value);
				break;
			case 4:
				$this->setIsPengawas($value);
				break;
			case 5:
				$this->setNpk($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = DosenPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setKodeDosen($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setNama($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setStatus($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setKodeJur($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setIsPengawas($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setNpk($arr[$keys[5]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(DosenPeer::DATABASE_NAME);

		if ($this->isColumnModified(DosenPeer::KODE_DOSEN)) $criteria->add(DosenPeer::KODE_DOSEN, $this->kode_dosen);
		if ($this->isColumnModified(DosenPeer::NAMA)) $criteria->add(DosenPeer::NAMA, $this->nama);
		if ($this->isColumnModified(DosenPeer::STATUS)) $criteria->add(DosenPeer::STATUS, $this->status);
		if ($this->isColumnModified(DosenPeer::KODE_JUR)) $criteria->add(DosenPeer::KODE_JUR, $this->kode_jur);
		if ($this->isColumnModified(DosenPeer::IS_PENGAWAS)) $criteria->add(DosenPeer::IS_PENGAWAS, $this->is_pengawas);
		if ($this->isColumnModified(DosenPeer::NPK)) $criteria->add(DosenPeer::NPK, $this->npk);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(DosenPeer::DATABASE_NAME);

		$criteria->add(DosenPeer::KODE_DOSEN, $this->kode_dosen);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     string
	 */
	public function getPrimaryKey()
	{
		return $this->getKodeDosen();
	}

	/**
	 * Generic method to set the primary key (kode_dosen column).
	 *
	 * @param      string $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setKodeDosen($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Dosen (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setKodeDosen($this->kode_dosen);

		$copyObj->setNama($this->nama);

		$copyObj->setStatus($this->status);

		$copyObj->setKodeJur($this->kode_jur);

		$copyObj->setIsPengawas($this->is_pengawas);

		$copyObj->setNpk($this->npk);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getDosenKelass() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addDosenKelas($relObj->copy($deepCopy));
				}
			}

			$relObj = $this->getDosenNonJaga();
			if ($relObj) {
				$copyObj->setDosenNonJaga($relObj->copy($deepCopy));
			}

			foreach ($this->getJadwalRuangs() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addJadwalRuang($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getPiketUjians() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addPiketUjian($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getSoalUjians() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addSoalUjian($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Dosen Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     DosenPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new DosenPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Jurusan object.
	 *
	 * @param      Jurusan $v
	 * @return     Dosen The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setJurusan(Jurusan $v = null)
	{
		if ($v === null) {
			$this->setKodeJur(NULL);
		} else {
			$this->setKodeJur($v->getKodeJur());
		}

		$this->aJurusan = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Jurusan object, it will not be re-added.
		if ($v !== null) {
			$v->addDosen($this);
		}

		return $this;
	}


	/**
	 * Get the associated Jurusan object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Jurusan The associated Jurusan object.
	 * @throws     PropelException
	 */
	public function getJurusan(PropelPDO $con = null)
	{
		if ($this->aJurusan === null && (($this->kode_jur !== "" && $this->kode_jur !== null))) {
			$this->aJurusan = JurusanPeer::retrieveByPk($this->kode_jur);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aJurusan->addDosens($this);
			 */
		}
		return $this->aJurusan;
	}

	/**
	 * Clears out the collDosenKelass collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addDosenKelass()
	 */
	public function clearDosenKelass()
	{
		$this->collDosenKelass = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collDosenKelass collection (array).
	 *
	 * By default this just sets the collDosenKelass collection to an empty array (like clearcollDosenKelass());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initDosenKelass()
	{
		$this->collDosenKelass = array();
	}

	/**
	 * Gets an array of DosenKelas objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Dosen has previously been saved, it will retrieve
	 * related DosenKelass from storage. If this Dosen is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array DosenKelas[]
	 * @throws     PropelException
	 */
	public function getDosenKelass($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDosenKelass === null) {
			if ($this->isNew()) {
			   $this->collDosenKelass = array();
			} else {

				$criteria->add(DosenKelasPeer::KODE_DOSEN, $this->kode_dosen);

				DosenKelasPeer::addSelectColumns($criteria);
				$this->collDosenKelass = DosenKelasPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(DosenKelasPeer::KODE_DOSEN, $this->kode_dosen);

				DosenKelasPeer::addSelectColumns($criteria);
				if (!isset($this->lastDosenKelasCriteria) || !$this->lastDosenKelasCriteria->equals($criteria)) {
					$this->collDosenKelass = DosenKelasPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastDosenKelasCriteria = $criteria;
		return $this->collDosenKelass;
	}

	/**
	 * Returns the number of related DosenKelas objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related DosenKelas objects.
	 * @throws     PropelException
	 */
	public function countDosenKelass(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collDosenKelass === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(DosenKelasPeer::KODE_DOSEN, $this->kode_dosen);

				$count = DosenKelasPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(DosenKelasPeer::KODE_DOSEN, $this->kode_dosen);

				if (!isset($this->lastDosenKelasCriteria) || !$this->lastDosenKelasCriteria->equals($criteria)) {
					$count = DosenKelasPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collDosenKelass);
				}
			} else {
				$count = count($this->collDosenKelass);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a DosenKelas object to this object
	 * through the DosenKelas foreign key attribute.
	 *
	 * @param      DosenKelas $l DosenKelas
	 * @return     void
	 * @throws     PropelException
	 */
	public function addDosenKelas(DosenKelas $l)
	{
		if ($this->collDosenKelass === null) {
			$this->initDosenKelass();
		}
		if (!in_array($l, $this->collDosenKelass, true)) { // only add it if the **same** object is not already associated
			array_push($this->collDosenKelass, $l);
			$l->setDosen($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Dosen is new, it will return
	 * an empty collection; or if this Dosen has previously
	 * been saved, it will retrieve related DosenKelass from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Dosen.
	 */
	public function getDosenKelassJoinKelasMK($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collDosenKelass === null) {
			if ($this->isNew()) {
				$this->collDosenKelass = array();
			} else {

				$criteria->add(DosenKelasPeer::KODE_DOSEN, $this->kode_dosen);

				$this->collDosenKelass = DosenKelasPeer::doSelectJoinKelasMK($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(DosenKelasPeer::KODE_DOSEN, $this->kode_dosen);

			if (!isset($this->lastDosenKelasCriteria) || !$this->lastDosenKelasCriteria->equals($criteria)) {
				$this->collDosenKelass = DosenKelasPeer::doSelectJoinKelasMK($criteria, $con, $join_behavior);
			}
		}
		$this->lastDosenKelasCriteria = $criteria;

		return $this->collDosenKelass;
	}

	/**
	 * Gets a single DosenNonJaga object, which is related to this object by a one-to-one relationship.
	 *
	 * @param      PropelPDO $con
	 * @return     DosenNonJaga
	 * @throws     PropelException
	 */
	public function getDosenNonJaga(PropelPDO $con = null)
	{

		if ($this->singleDosenNonJaga === null && !$this->isNew()) {
			$this->singleDosenNonJaga = DosenNonJagaPeer::retrieveByPK($this->kode_dosen, $con);
		}

		return $this->singleDosenNonJaga;
	}

	/**
	 * Sets a single DosenNonJaga object as related to this object by a one-to-one relationship.
	 *
	 * @param      DosenNonJaga $l DosenNonJaga
	 * @return     Dosen The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setDosenNonJaga(DosenNonJaga $v)
	{
		$this->singleDosenNonJaga = $v;

		// Make sure that that the passed-in DosenNonJaga isn't already associated with this object
		if ($v->getDosen() === null) {
			$v->setDosen($this);
		}

		return $this;
	}

	/**
	 * Clears out the collJadwalRuangs collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addJadwalRuangs()
	 */
	public function clearJadwalRuangs()
	{
		$this->collJadwalRuangs = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collJadwalRuangs collection (array).
	 *
	 * By default this just sets the collJadwalRuangs collection to an empty array (like clearcollJadwalRuangs());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initJadwalRuangs()
	{
		$this->collJadwalRuangs = array();
	}

	/**
	 * Gets an array of JadwalRuang objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Dosen has previously been saved, it will retrieve
	 * related JadwalRuangs from storage. If this Dosen is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array JadwalRuang[]
	 * @throws     PropelException
	 */
	public function getJadwalRuangs($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJadwalRuangs === null) {
			if ($this->isNew()) {
			   $this->collJadwalRuangs = array();
			} else {

				$criteria->add(JadwalRuangPeer::KODE_DOSEN, $this->kode_dosen);

				JadwalRuangPeer::addSelectColumns($criteria);
				$this->collJadwalRuangs = JadwalRuangPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(JadwalRuangPeer::KODE_DOSEN, $this->kode_dosen);

				JadwalRuangPeer::addSelectColumns($criteria);
				if (!isset($this->lastJadwalRuangCriteria) || !$this->lastJadwalRuangCriteria->equals($criteria)) {
					$this->collJadwalRuangs = JadwalRuangPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastJadwalRuangCriteria = $criteria;
		return $this->collJadwalRuangs;
	}

	/**
	 * Returns the number of related JadwalRuang objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related JadwalRuang objects.
	 * @throws     PropelException
	 */
	public function countJadwalRuangs(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collJadwalRuangs === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(JadwalRuangPeer::KODE_DOSEN, $this->kode_dosen);

				$count = JadwalRuangPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(JadwalRuangPeer::KODE_DOSEN, $this->kode_dosen);

				if (!isset($this->lastJadwalRuangCriteria) || !$this->lastJadwalRuangCriteria->equals($criteria)) {
					$count = JadwalRuangPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collJadwalRuangs);
				}
			} else {
				$count = count($this->collJadwalRuangs);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a JadwalRuang object to this object
	 * through the JadwalRuang foreign key attribute.
	 *
	 * @param      JadwalRuang $l JadwalRuang
	 * @return     void
	 * @throws     PropelException
	 */
	public function addJadwalRuang(JadwalRuang $l)
	{
		if ($this->collJadwalRuangs === null) {
			$this->initJadwalRuangs();
		}
		if (!in_array($l, $this->collJadwalRuangs, true)) { // only add it if the **same** object is not already associated
			array_push($this->collJadwalRuangs, $l);
			$l->setDosen($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Dosen is new, it will return
	 * an empty collection; or if this Dosen has previously
	 * been saved, it will retrieve related JadwalRuangs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Dosen.
	 */
	public function getJadwalRuangsJoinRuang($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJadwalRuangs === null) {
			if ($this->isNew()) {
				$this->collJadwalRuangs = array();
			} else {

				$criteria->add(JadwalRuangPeer::KODE_DOSEN, $this->kode_dosen);

				$this->collJadwalRuangs = JadwalRuangPeer::doSelectJoinRuang($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(JadwalRuangPeer::KODE_DOSEN, $this->kode_dosen);

			if (!isset($this->lastJadwalRuangCriteria) || !$this->lastJadwalRuangCriteria->equals($criteria)) {
				$this->collJadwalRuangs = JadwalRuangPeer::doSelectJoinRuang($criteria, $con, $join_behavior);
			}
		}
		$this->lastJadwalRuangCriteria = $criteria;

		return $this->collJadwalRuangs;
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Dosen is new, it will return
	 * an empty collection; or if this Dosen has previously
	 * been saved, it will retrieve related JadwalRuangs from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Dosen.
	 */
	public function getJadwalRuangsJoinKaryawan($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collJadwalRuangs === null) {
			if ($this->isNew()) {
				$this->collJadwalRuangs = array();
			} else {

				$criteria->add(JadwalRuangPeer::KODE_DOSEN, $this->kode_dosen);

				$this->collJadwalRuangs = JadwalRuangPeer::doSelectJoinKaryawan($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(JadwalRuangPeer::KODE_DOSEN, $this->kode_dosen);

			if (!isset($this->lastJadwalRuangCriteria) || !$this->lastJadwalRuangCriteria->equals($criteria)) {
				$this->collJadwalRuangs = JadwalRuangPeer::doSelectJoinKaryawan($criteria, $con, $join_behavior);
			}
		}
		$this->lastJadwalRuangCriteria = $criteria;

		return $this->collJadwalRuangs;
	}

	/**
	 * Clears out the collPiketUjians collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addPiketUjians()
	 */
	public function clearPiketUjians()
	{
		$this->collPiketUjians = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collPiketUjians collection (array).
	 *
	 * By default this just sets the collPiketUjians collection to an empty array (like clearcollPiketUjians());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initPiketUjians()
	{
		$this->collPiketUjians = array();
	}

	/**
	 * Gets an array of PiketUjian objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Dosen has previously been saved, it will retrieve
	 * related PiketUjians from storage. If this Dosen is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array PiketUjian[]
	 * @throws     PropelException
	 */
	public function getPiketUjians($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collPiketUjians === null) {
			if ($this->isNew()) {
			   $this->collPiketUjians = array();
			} else {

				$criteria->add(PiketUjianPeer::KODE_DOSEN, $this->kode_dosen);

				PiketUjianPeer::addSelectColumns($criteria);
				$this->collPiketUjians = PiketUjianPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(PiketUjianPeer::KODE_DOSEN, $this->kode_dosen);

				PiketUjianPeer::addSelectColumns($criteria);
				if (!isset($this->lastPiketUjianCriteria) || !$this->lastPiketUjianCriteria->equals($criteria)) {
					$this->collPiketUjians = PiketUjianPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastPiketUjianCriteria = $criteria;
		return $this->collPiketUjians;
	}

	/**
	 * Returns the number of related PiketUjian objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related PiketUjian objects.
	 * @throws     PropelException
	 */
	public function countPiketUjians(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collPiketUjians === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(PiketUjianPeer::KODE_DOSEN, $this->kode_dosen);

				$count = PiketUjianPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(PiketUjianPeer::KODE_DOSEN, $this->kode_dosen);

				if (!isset($this->lastPiketUjianCriteria) || !$this->lastPiketUjianCriteria->equals($criteria)) {
					$count = PiketUjianPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collPiketUjians);
				}
			} else {
				$count = count($this->collPiketUjians);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a PiketUjian object to this object
	 * through the PiketUjian foreign key attribute.
	 *
	 * @param      PiketUjian $l PiketUjian
	 * @return     void
	 * @throws     PropelException
	 */
	public function addPiketUjian(PiketUjian $l)
	{
		if ($this->collPiketUjians === null) {
			$this->initPiketUjians();
		}
		if (!in_array($l, $this->collPiketUjians, true)) { // only add it if the **same** object is not already associated
			array_push($this->collPiketUjians, $l);
			$l->setDosen($this);
		}
	}

	/**
	 * Clears out the collSoalUjians collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addSoalUjians()
	 */
	public function clearSoalUjians()
	{
		$this->collSoalUjians = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collSoalUjians collection (array).
	 *
	 * By default this just sets the collSoalUjians collection to an empty array (like clearcollSoalUjians());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initSoalUjians()
	{
		$this->collSoalUjians = array();
	}

	/**
	 * Gets an array of SoalUjian objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Dosen has previously been saved, it will retrieve
	 * related SoalUjians from storage. If this Dosen is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array SoalUjian[]
	 * @throws     PropelException
	 */
	public function getSoalUjians($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSoalUjians === null) {
			if ($this->isNew()) {
			   $this->collSoalUjians = array();
			} else {

				$criteria->add(SoalUjianPeer::KODE_DOSEN, $this->kode_dosen);

				SoalUjianPeer::addSelectColumns($criteria);
				$this->collSoalUjians = SoalUjianPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(SoalUjianPeer::KODE_DOSEN, $this->kode_dosen);

				SoalUjianPeer::addSelectColumns($criteria);
				if (!isset($this->lastSoalUjianCriteria) || !$this->lastSoalUjianCriteria->equals($criteria)) {
					$this->collSoalUjians = SoalUjianPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastSoalUjianCriteria = $criteria;
		return $this->collSoalUjians;
	}

	/**
	 * Returns the number of related SoalUjian objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related SoalUjian objects.
	 * @throws     PropelException
	 */
	public function countSoalUjians(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collSoalUjians === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(SoalUjianPeer::KODE_DOSEN, $this->kode_dosen);

				$count = SoalUjianPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(SoalUjianPeer::KODE_DOSEN, $this->kode_dosen);

				if (!isset($this->lastSoalUjianCriteria) || !$this->lastSoalUjianCriteria->equals($criteria)) {
					$count = SoalUjianPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collSoalUjians);
				}
			} else {
				$count = count($this->collSoalUjians);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a SoalUjian object to this object
	 * through the SoalUjian foreign key attribute.
	 *
	 * @param      SoalUjian $l SoalUjian
	 * @return     void
	 * @throws     PropelException
	 */
	public function addSoalUjian(SoalUjian $l)
	{
		if ($this->collSoalUjians === null) {
			$this->initSoalUjians();
		}
		if (!in_array($l, $this->collSoalUjians, true)) { // only add it if the **same** object is not already associated
			array_push($this->collSoalUjians, $l);
			$l->setDosen($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Dosen is new, it will return
	 * an empty collection; or if this Dosen has previously
	 * been saved, it will retrieve related SoalUjians from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Dosen.
	 */
	public function getSoalUjiansJoinKelasMK($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(DosenPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collSoalUjians === null) {
			if ($this->isNew()) {
				$this->collSoalUjians = array();
			} else {

				$criteria->add(SoalUjianPeer::KODE_DOSEN, $this->kode_dosen);

				$this->collSoalUjians = SoalUjianPeer::doSelectJoinKelasMK($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(SoalUjianPeer::KODE_DOSEN, $this->kode_dosen);

			if (!isset($this->lastSoalUjianCriteria) || !$this->lastSoalUjianCriteria->equals($criteria)) {
				$this->collSoalUjians = SoalUjianPeer::doSelectJoinKelasMK($criteria, $con, $join_behavior);
			}
		}
		$this->lastSoalUjianCriteria = $criteria;

		return $this->collSoalUjians;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collDosenKelass) {
				foreach ((array) $this->collDosenKelass as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->singleDosenNonJaga) {
				$this->singleDosenNonJaga->clearAllReferences($deep);
			}
			if ($this->collJadwalRuangs) {
				foreach ((array) $this->collJadwalRuangs as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collPiketUjians) {
				foreach ((array) $this->collPiketUjians as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collSoalUjians) {
				foreach ((array) $this->collSoalUjians as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collDosenKelass = null;
		$this->singleDosenNonJaga = null;
		$this->collJadwalRuangs = null;
		$this->collPiketUjians = null;
		$this->collSoalUjians = null;
			$this->aJurusan = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseDosen:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseDosen::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseDosen
